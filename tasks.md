- [ ] jump animation (jump is supposed to change the players state, meaning you will have to press walk (`r`/`t`) again to start walking)

- [x] lookup ratatui Canvas::default()
  - [x] .draw()
  - [ ] .layer()


- [ ] prevent key inputs from changing gameloop speed


## cool stuff

- canvas
  - you can draw shaps
  - you can create your own shapes (maybe this is how we render characters)
